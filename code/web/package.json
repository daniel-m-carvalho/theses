{
  "name": "phylodelta-web",
  "version": "0.1.0",
  "private": true,
  "description": "The PhyloDelta application: side-by-side tree comparison driven by server-computed slices.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": "^20.19.0 || >=22.12.0" },
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test"
  },
  "//dependencies": "sigma and graphology are peerDependencies of phylo-tree-viewer, so the consumer supplies them; two copies in a bundle would produce objects that are not interchangeable.",
  "dependencies": {
    "graphology": "^0.26.0",
    "phylo-tree-viewer": "*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "sigma": "^3.0.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.0",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
