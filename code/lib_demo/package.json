{
  "name": "lib-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Demo application for phylo-tree-viewer: two Vibrio trees side by side with isolate bar charts, metadata filtering and difference colouring.",
  "license": "MIT",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test"
  },
  "//dependencies": "sigma and graphology are listed here because phylo-tree-viewer declares them as peerDependencies — the consumer supplies them, so one shared instance reaches the library. iwanthue is the demo's own (runtime palette generation).",
  "dependencies": {
    "graphology": "^0.26.0",
    "iwanthue": "^2.1.0",
    "phylo-tree-viewer": "*",
    "sigma": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.5",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
