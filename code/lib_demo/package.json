{
  "name": "lib-demo",
  "private": true,
  "description": "Demo app for the phylogenetic tree-viz library. Scripts only: dependencies and node_modules live in the repo-root package.json, so install from the repo root (npm install), not here.",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test"
  }
}
